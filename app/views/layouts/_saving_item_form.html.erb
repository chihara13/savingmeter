<%= form_for(@saving_item) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new micropost..." %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>